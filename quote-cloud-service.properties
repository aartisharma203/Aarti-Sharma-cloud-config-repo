# this is the port on which our hello-cloud-service will run
server.port=2244

# allow for RefreshScope
management.endpoints.web.exposure.include=*

# the quote-cloud-service will use this as the greeting that it serves up
quote="Hello Quote!"
